# Source provenance

The initial local helper archive was supplied by the user on 2026-06-26.
The user independently confirmed that the archived executable and the installed
`/Users/easonhuo/bin/drpo-update` were byte-identical using both `cmp` and SHA-256.

Verified executable SHA-256:

```text
f344b0cffc163ecdeb80ec8d07b564d00c3538ad22e03887f87bd1ce2a85f4f3
```

The repository version begins from that verified source lineage and upgrades it
to the transactional Stage 1 Git-bundle workflow. Git history is the canonical
record of subsequent changes.
