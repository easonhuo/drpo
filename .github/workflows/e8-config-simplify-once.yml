name: E8 Config Simplify Once

on:
  push:
    branches: [dev/e8-config-driven-sweep-01]

permissions:
  contents: write

jobs:
  simplify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev/e8-config-driven-sweep-01
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Apply subtraction refactor
        run: bash scripts/.e8_config_simplify_once.sh

      - name: Normalize new-file lint contracts
        run: |
          python - <<'PY'
          from pathlib import Path

          preflight = Path("scripts/preflight_e8_multitask_config.py")
          text = preflight.read_text(encoding="utf-8")
          if text.startswith("#!/usr/bin/env python3\n"):
              text = text.split("\n", 1)[1]
          preflight.write_text(text, encoding="utf-8")

          config = Path("src/drpo/e8_experiment_config.py")
          text = config.read_text(encoding="utf-8")
          for line in (
              'raise ValueError(f"{label} must be a mapping")',
              'raise ValueError(f"{label} must be a sequence")',
              'raise ValueError(f"{label} must be an integer")',
              'raise ValueError(f"{label} must be a finite numeric scalar")',
              'raise ValueError(f"{label} must be boolean")',
          ):
              text = text.replace(line, line + "  # noqa: TRY004", 1)
          config.write_text(text, encoding="utf-8")
          PY

      - name: Install development dependencies
        run: python -m pip install -e '.[dev]'

      - name: Format changed Python
        run: |
          ruff format \
            src/drpo/e8_experiment_config.py \
            scripts/preflight_e8_multitask_config.py \
            src/drpo/e8_multitask_exp_tuning.py \
            tests/test_e8_multitask_p0.py

      - name: Engineering regression checks
        run: |
          python -m py_compile \
            src/drpo/e8_experiment_config.py \
            scripts/preflight_e8_multitask_config.py \
            src/drpo/e8_multitask_exp_tuning.py \
            tests/test_e8_multitask_p0.py
          bash -n scripts/run_e8_multitask_exp_coldstart.sh
          bash -n scripts/bootstrap_e8_multitask_exp_coldstart.sh
          bash -n scripts/run_e8_multitask_exp_lambda_completion.sh
          ruff check \
            src/drpo/e8_experiment_config.py \
            scripts/preflight_e8_multitask_config.py \
            tests/test_e8_multitask_p0.py
          python -m pytest -q tests/test_e8_multitask_p0.py
          git diff --check

      - name: Commit simplified implementation and remove one-shot machinery
        run: |
          rm .github/workflows/e8-config-simplify-once.yml
          rm scripts/.e8_config_simplify_once.sh
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "refactor: simplify E8 config-driven sweep authority"
          git push origin HEAD:dev/e8-config-driven-sweep-01
