name: E8 Multitask AsymRE Capability Repair (superseded)

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by e8-multitask-asymre-capability-finalize.yml"
