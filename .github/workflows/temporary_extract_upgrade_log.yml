name: Temporary extract upgrade log

on:
  push:
    branches:
      - diag/e8-upgrade-log-20260720
    paths:
      - .github/workflows/temporary_extract_upgrade_log.yml

permissions:
  actions: read
  contents: write

jobs:
  extract:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: diag/e8-upgrade-log-20260720
      - name: Extract log tail
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p docs/experiments
          gh run view 29732469210 --repo easonhuo/drpo --log-failed > /tmp/log.txt 2>&1 || true
          tail -n 120 /tmp/log.txt > docs/experiments/E8_UPGRADE_LOG_TAIL.txt
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/temporary_extract_upgrade_log.yml
          git add docs/experiments/E8_UPGRADE_LOG_TAIL.txt
          git commit -m 'Extract E8 finalizer upgrade log tail'
          git push origin HEAD:diag/e8-upgrade-log-20260720
