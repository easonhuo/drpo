name: Temporary E8 repaired-finalizer failure extractor

on:
  push:
    branches:
      - diag/e8-reciprocal-finalizer-48a3e49
    paths:
      - .github/workflows/temporary_e8_reciprocal_status_probe.yml

permissions:
  actions: read
  contents: write

jobs:
  extract:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: diag/e8-reciprocal-finalizer-48a3e49
      - name: Extract repaired-finalizer failure
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p docs/experiments
          gh run view 29730766310 --repo easonhuo/drpo --log-failed > /tmp/finalizer_failed_2.log 2>&1 || true
          {
            echo 'run_id=29730766310'
            grep -Ein 'ERROR|Error|FAILED|failed|Traceback|Process completed|assert|mismatch|missing|KeyError|FileNotFoundError' /tmp/finalizer_failed_2.log | tail -n 160 || true
            echo '--- tail ---'
            tail -n 100 /tmp/finalizer_failed_2.log
          } > docs/experiments/TEMP_RECIPROCAL_FINALIZER_FAILURE_2.txt
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/experiments/TEMP_RECIPROCAL_FINALIZER_FAILURE_2.txt
          git commit -m 'Extract repaired reciprocal finalizer failure'
          git push origin HEAD:diag/e8-reciprocal-finalizer-48a3e49
