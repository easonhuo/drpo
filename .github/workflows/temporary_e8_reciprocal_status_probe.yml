name: Temporary E8 backlog recovery status probe

on:
  push:
    branches:
      - diag/e8-reciprocal-finalizer-48a3e49
    paths:
      - .github/workflows/temporary_e8_reciprocal_status_probe.yml

permissions:
  actions: read
  contents: write

jobs:
  probe:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: diag/e8-reciprocal-finalizer-48a3e49
      - name: Record recovery state
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api 'repos/easonhuo/drpo/actions/runs?head_sha=485157144a378e882db1043ddcae459cc58ab87e&per_page=100' > /tmp/runs.json
          python - <<'PY'
          import json
          from pathlib import Path
          payload = json.loads(Path('/tmp/runs.json').read_text())
          rows = [row for row in payload['workflow_runs'] if row['name'] == 'Temporary E8 backlog recovery audit']
          if not rows:
              raise SystemExit('recovery run not found')
          row = rows[0]
          Path('docs/experiments/TEMP_BACKLOG_RECOVERY_STATUS.txt').write_text(
              f"run_id={row['id']}\nstatus={row['status']}\nconclusion={row['conclusion']}\n",
              encoding='utf-8',
          )
          PY
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/experiments/TEMP_BACKLOG_RECOVERY_STATUS.txt
          git commit -m 'Record backlog recovery state'
          git push origin HEAD:diag/e8-reciprocal-finalizer-48a3e49
