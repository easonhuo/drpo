-update magnitude into near- and
 far-field components, and dashed traces report held-out-context reward.
-\textbf{(b)} Reserved external validation summary for Hopper and Countdown;
-dashes mark outcomes awaiting the corresponding formal runs.
+\textbf{(b)} Fixed-budget Countdown-0.5B transfer summary and reserved Hopper panel. Hopper entries remain open pending the formal run and terminal audit. Countdown entries report the maximum two-seed late-window Pass@8 over each method\'s registered coefficient sweep; complete coefficient-response curves are shown in Appendix~\ref{fig:app_countdown_taper_coefficient_response}.
 }
-\Description{Composite figure. The controlled panel compares near-field and far-field retained negative-update magnitude under matched taper operating points. The transfer panel is a reserved table for Hopper return and Countdown success.}
+\Description{Composite figure. The controlled panel compares near-field and far-field retained negative-update magnitude under matched taper operating points. The transfer panel reports fixed-budget Countdown late-window Pass@8 for positive-only, global-alpha, reciprocal-linear, reciprocal-quadratic, and DRPO, while Hopper cells remain pending.}
 \label{fig:taper_control_and_transfer}
 \end{figure}
 
@@ -1361,7 +1360,11 @@ tapering introduces remoteness-aware suppression, but its tail remains
 heavier than the exponential rule. The exponential taper instead retains
 near-field repulsion while rapidly suppressing the far-field tail,
 yielding higher task performance and held-out-context generalization
-than the matched alternatives.
+than the matched alternatives. In the fixed-budget Countdown transfer
+summary, DRPO attains the strongest late-window Pass@8 (15.7\%),
+followed by Reciprocal-Quadratic (14.5\%), Positive-only (14.0\%),
+Reciprocal-Linear (13.6\%), and Global-$\alpha$ (2.4\%); the complete
+coefficient-response curves are reported in Appendix~\ref{fig:app_countdown_taper_coefficient_response}.
 
 \