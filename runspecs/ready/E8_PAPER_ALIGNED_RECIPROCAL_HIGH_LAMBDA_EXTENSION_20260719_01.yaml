version: 1
run_id: E8_PAPER_ALIGNED_RECIPROCAL_HIGH_LAMBDA_EXTENSION_20260719_01
lane: e8
priority: 151
created_at: "2026-07-19T11:00:00Z"
experiment_id: EXT-C-E8-ORACLE-OFFLINE-V2-PAPER-ALIGNED-RECIPROCAL-HIGH-LAMBDA-EXTENSION-0.5B-01
repo_commit: f8ec63cd3d079cbf809c9ffa9a0e149312881ccb

registration:
  mode: deferred
  closure_required: true

provenance:
  commit_policy: protected_paths_unchanged
  protected_paths:
    - configs/countdown_e8_oracle_offline_v2_reciprocal_high_lambda_extension_0p5b.yaml
    - src/drpo/countdown_e8_alpha1_highc_scan_common.py
    - src/drpo/countdown_e8_alpha1_c_scan_trainer.py
    - src/drpo/countdown_e8_alpha1_c_scan_runtime.py
    - src/drpo/countdown_e8_alpha1_highc_scan_runtime.py
    - scripts/run_countdown_e8_oracle_offline_v2_alpha1_c_scan_auto.py
    - scripts/run_countdown_e8_oracle_offline_v2_alpha1_highc_scan_auto.py
    - scripts/run_countdown_e8_oracle_offline_v2_alpha1_highc_scan_auto_one_click.sh
    - tests/test_countdown_e8_oracle_offline_v2_alpha1_highc_scan.py

purpose: >
  Extend the completed manuscript-correct reciprocal shape screen to larger
  lambda values without changing the scientific protocol. Run
  Reciprocal-Linear and Reciprocal-Quadratic at lambda 39, 79, 159, and 319 on
  the same two paired development seed offsets, for exactly eight method
  points and sixteen cells. Do not rerun the completed lambda<=19 reciprocal
  cells, Positive-only, Global, or the exponential anchor. This code-first
  pilot remains validation-only and cannot establish convergence, steady
  state, significance, or a formal method ranking.

entrypoint:
  cwd: repo_root
  command: >-
    E8_ALPHA1_HIGHC_WORK_DIR=outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001
    E8_ALPHA1_HIGHC_GRID_CONFIG=configs/countdown_e8_oracle_offline_v2_reciprocal_high_lambda_extension_0p5b.yaml
    bash scripts/run_countdown_e8_oracle_offline_v2_alpha1_highc_scan_auto_one_click.sh

recovery:
  enabled: true
  max_attempts: 2
  resume_command: >-
    E8_ALPHA1_HIGHC_WORK_DIR=outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001
    E8_ALPHA1_HIGHC_GRID_CONFIG=configs/countdown_e8_oracle_offline_v2_reciprocal_high_lambda_extension_0p5b.yaml
    bash scripts/run_countdown_e8_oracle_offline_v2_alpha1_highc_scan_auto_one_click.sh
  retryable_exit_codes: [137, 143]
  checkpoint_globs:
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/RUNTIME_SELECTION.json
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/RUNTIME_SLOTS.json
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/SWEEP_STATUS.json
  backoff_seconds: 60

policy:
  existing_script_required: true
  forbid_new_launcher: true
  forbid_hparam_change: true
  forbid_cross_lane: true

outputs:
  run_dir: outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001
  summary_file: outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/SWEEP_COMPLETE.json
  audit_file: outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/aggregate/terminal_audit.json

success_criteria:
  - shared coordinate is x=relu(current_mean_token_surprisal/2-0.125) and is detached
  - Reciprocal-Linear remains 1/(1+lambda*sqrt(x)) with no formula change
  - Reciprocal-Quadratic remains 1/(1+lambda*x) with no extra square
  - matrix contains 8 method points and 16 cells on paired seed offsets 4000 and 5000
  - each family uses lambda values 39, 79, 159, and 319 exactly once per seed
  - completed reciprocal lambda values 1, 3, 7, and 19 are historical references and are not rerun
  - no Positive-only, Global, or exponential cell is launched in this round
  - previous reciprocal result identity remains bound to drpo-results commit 8d04768273315ff6ab1bcbade9cbf5705952259a
  - runtime records GPU 0-7, two slots per GPU, and 16 concurrent cells
  - the full sweep requires one 16-slot wave after the smoke gate
  - smoke gate uses reciprocal_quadratic lambda 79 before the full sweep and is not scientific evidence
  - test_data_used remains false
  - task performance, support or structure boundary, and NaN/Inf failure are reported separately
  - fixed 1200 steps is not described as convergence, steady state, significance, or ranking
  - completed text-first evidence is delivered to drpo-results ingest/e8

artifacts:
  package_policy: manifest_only
  include:
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/*.json
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/aggregate/*.json
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/aggregate/*.csv
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/methods/*/summary.json
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/methods/*/metrics.csv
    - outputs/e8/paper_aligned_reciprocal_high_lambda_extension_001/logs/*.log
  exclude:
    - "**/*.pt"
    - "**/*.pth"
    - "**/*.ckpt"
    - "**/*.safetensors"
    - "**/*.bin"
    - "**/checkpoint*"
    - "**/checkpoints/**"
    - "**/optimizer*"
    - "**/wandb/**"
  max_package_size_mb: 100
  fail_if_excluded_matched: false
  fail_if_package_too_large: true

delivery:
  enabled: true
  auto: true
  mode: results_repo
  repository: easonhuo/drpo-results
  branch: ingest/e8
  export_profile: manifest_text_v1
  max_total_size_mb: 30
  max_file_size_mb: 10

publish:
  enabled: false
  auto: false
